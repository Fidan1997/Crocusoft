import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#000",
      light: "#f7f7f7"
    },
    secondary: {
      main: "#f7f7f7",
      light: "#000",
    },
  },
});

export default theme;
